## 为什么

<!-- 解释此变更的动机。这解决了什么问题？为什么现在要做？ -->

## 变更内容

<!-- 描述将发生的变化。具体说明新功能、修改或移除的内容。 -->

## 功能

### 新功能
<!-- 引入的功能。将 <name> 替换为 kebab-case 标识符（例如 user-auth、data-export、api-rate-limiting）。每个功能创建 specs/<name>/spec.md -->
- `<name>`: <简要描述此功能涵盖的内容>

### 修改的功能
<!-- 现有功能的需求正在发生变化（而不仅仅是实现）。
     仅在规范级行为发生变化时在此处列出。每个功能需要一个增量规范文件。
     使用 msdd/specs/ 中的现有规范名称。如果没有需求变化，则留空。
     完全没有功能变更的更改（纯重构、工具、文档）必须在其 .msdd.yaml 中设置 `skip_specs: true`，
     msdd validate 会拒绝没有该标记的零增量更改。不要仅为满足验证而创建需求。 -->
- `<existing-name>`: <正在变更的需求>

## 影响

<!-- 受影响的子仓（列出 codebase/ 下的子仓）、跨仓接口、依赖、系统 -->
